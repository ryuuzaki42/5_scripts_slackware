# Backups created by editors like emacs, nano or vi
*~

# Temporary files which can be created if a process still has a handle open of a deleted file
.fuse_hidden*

# KDE directory preferences
.directory

# Linux trash folder which might appear on any partition or disk
.Trash-*

# Files are created when an open file is removed but is still being accessed
.nfs*

# swap vim files
.*.swp

# Kwrite/Kate temporary/swap files
*.kate-swp
